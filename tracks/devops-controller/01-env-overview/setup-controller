# #!/bin/bash

# rm -rf /home/rhel/acme_corp
# mkdir /home/rhel/acme_corp && cd /home/rhel/acme_corp
# git config --global init.defaultBranch main
# git init
# git config credential.helper 'store --file /tmp/git-creds'
# echo http://student:learn_ansible@gitea%3a3000 > /tmp/git-creds
# echo $?
# whoami
# ls -alh /tmp/git-creds
# git remote add origin http://gitea:3000/student/acme_corp.git
# git checkout -b main
# /usr/bin/tar zxvf /tmp/setup-scripts/devops-controller/files/acme_repo.tar.gz
# git config user.email student@rhel
# git config user.name student
# git add .
# git commit -m"Initial commit"
# git push -u origin main --force
# chown -hR rhel:rhel /home/rhel/acme_corp/
# chown rhel:rhel /tmp/git-creds